<?xml version="1.0" encoding="utf-8"?>
<!-- Animated focus effect for movie cards -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused AND Selected state - Special combined effect -->
    <item android:state_focused="true" android:state_selected="true">
        <layer-list>
            <!-- Outer glow (Red for selected) -->
            <item android:left="-4dp" android:top="-4dp" android:right="-4dp" android:bottom="-4dp">
                <shape android:shape="rectangle">
                    <stroke android:width="6dp" android:color="#99E50914" />
                    <corners android:radius="8dp" />
                </shape>
            </item>
            <!-- Middle Red Border -->
            <item>
                <shape android:shape="rectangle">
                    <stroke android:width="4dp" android:color="@color/netflix_red" />
                    <corners android:radius="6dp" />
                </shape>
            </item>
            <!-- Inner White Border -->
            <item android:left="2dp" android:top="2dp" android:right="2dp" android:bottom="2dp">
                <shape android:shape="rectangle">
                    <stroke android:width="2dp" android:color="#FFFFFF" />
                    <corners android:radius="4dp" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Focused state with animated glow -->
    <item android:state_focused="true">
        <layer-list>
            <!-- Outer glow -->
            <item android:left="-4dp" android:top="-4dp" android:right="-4dp" android:bottom="-4dp">
                <shape android:shape="rectangle">
                    <stroke android:width="6dp" android:color="#66E50914" />
                    <corners android:radius="8dp" />
                </shape>
            </item>
            <!-- Inner bright border -->
            <item>
                <shape android:shape="rectangle">
                    <stroke android:width="3dp" android:color="#FFFFFF" />
                    <corners android:radius="6dp" />
                </shape>
            </item>
        </layer-list>
    </item>
    <!-- Selected state -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <stroke android:width="2dp" android:color="@color/netflix_red" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>
